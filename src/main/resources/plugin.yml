name: SimpleBossBar
version: '${version}'
main: dev.lyphium.bossbar.SimpleBossBar
api-version: '1.21'
prefix: SimpleBossBar
load: POSTWORLD
author: Lyphion
description: Simple Boss Bar plugin

softdepend: [PlaceholderAPI]

permissions:
  bossbar.admin:
    description: Admin permission
    default: op

commands:
  reloadbossbar:
    description: Reload the config
    usage: Please use /reloadbossbar
    permission: bossbar.admin
